const initialState={
    allCountries:[], // ??
    activities:[] //??
};


export const reducer=(state=initialState,action)=>{
    switch (action.type){
        default:
            return {...state}
    }
}